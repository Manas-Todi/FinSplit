#include <bits/stdc++.h>
typedef long long ll;
#define ld long double
# define qsort(v) sort(v.begin(),v.end())
# define greatsort(v) sort(v.begin(),v.end(),greater<>())
#define pb push_back
#define int ll
using namespace std;
//const ll MOD = 1e9+7;
//const ld PI = acos(-1);
//const ld EPS = 1e-9;
//const ll INF = 1e18;

void solve(){

    cout<<"HELLO WORLD"<<endl;

    return;
}

signed main(){
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    //cout.precision(15);
    ll TC = 1, t = 0;
    cin >> TC;
    while(t++ < TC){
        solve();
    }

    return 0;
}